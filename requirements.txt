fastapi>=0.115.8
uvicorn[standard]>=0.34.0
httpx>=0.27.2
orjson>=3.10.0
uvloop>=0.19.0
httptools>=0.6.0
ollama>=0.4.5

# TTS — Omnivoice (torch pre-installed on Kaggle/Colab)
omnivoice>=0.2.1

# TTS — Inflect (hf_hub downloads model, inference is the pip lib)
huggingface_hub
inference>=0.22
